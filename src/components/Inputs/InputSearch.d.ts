interface Props {
    id?: string;
    placeholder?: string;
    classNameContent?: string;
    classNameInput?: string;
    textLabel?: string;
    textButton?: string;
    onChange?: (props: any) => void;
}
export declare const InputSearch: ({ id, placeholder, classNameContent, classNameInput, textLabel, onChange, }: Props) => import("preact").JSX.Element;
export {};
